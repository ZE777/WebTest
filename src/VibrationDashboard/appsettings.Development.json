{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "// 開發者資料來源 = SQL": "開發環境（dotnet run）切 SQL：連本機 .\\SQLEXPRESS 的 WebTest，採 Windows 整合式驗證(Trusted_Connection)＝無帳密；你的登入帳號需有該庫存取權（當初建庫的帳號通常即可）。首次啟動自動套 Migrations 建表 + 從 App_Data 的 JSON 種子。前置：本機要有 .\\SQLEXPRESS 的 WebTest 庫。臨時改用 JSON 開發：dotnet run --DataSource:Type=Json。發布/正式版維持 JSON（見 appsettings.json / appsettings.Production.json）。",
  "DataSource": {
    "Type": "Sql"
  },
  "ConnectionStrings": {
    "Default": "Server=.\\SQLEXPRESS;Database=WebTest;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=true"
  }
}
