# 講稿 → 程式碼對照表（報告當天速查）

> 用途:報告/被追問時，依講稿提到的點快速找到對應程式碼。路徑為相對 repo 根目錄，後綴 `:行號` 為關鍵段落。

## 前端：技術組成

| 講稿提到 | 程式碼位置 |
|---|---|
| Razor 頁面結構 | `src/VibrationDashboard/Views/Home/Index.cshtml` + Partials（`_KpiBar` / `_OverviewPanels` / `_FilterBar` / `_MachineCardTemplate` / `_TrendModal` / `_AlertsPanel`）|
| IIFE 模組化 + 統一狀態源 | `src/VibrationDashboard/wwwroot/js/appState.js`（每個 js 檔都是 IIFE）|
| `textContent` 防 XSS | `src/VibrationDashboard/wwwroot/js/api.js:75`（Toast）、`trendChart.js` 的 `.text()` |
| jQuery AJAX 封裝 + 全域錯誤 | `src/VibrationDashboard/wwwroot/js/api.js` |

## 圖表 + 即時更新

| 講稿提到 | 程式碼位置 |
|---|---|
| ECharts `axisPointer`(游標) | `src/VibrationDashboard/wwwroot/js/trendChart.js:179` |
| ECharts `markLine`(門檻線) | `src/VibrationDashboard/wwwroot/js/trendChart.js:237` |
| ECharts `markArea`(分區色帶) | `src/VibrationDashboard/wwwroot/js/trendChart.js:245` |
| 懶加載 ECharts 函式庫 | `src/VibrationDashboard/wwwroot/js/trendChart.js:122-135` |
| SignalR 前端連線 + 降級輪詢 | `src/VibrationDashboard/wwwroot/js/realtime.js:47-66` |
| SignalR Hub（單向推播通道）| `src/VibrationDashboard/Hubs/MachineHub.cs` |
| SignalR 設定（camelCase + enum 字串）| `src/VibrationDashboard/Program.cs:64-69` |
| Hub 掛載端點 `/hubs/machine` | `src/VibrationDashboard/Program.cs:165` |
| 檔案監看 + 400ms 去抖（觸發鏈）| `src/VibrationDashboard/Services/Realtime/FileWatcherService.cs` |
| 推單台 delta（`IHubContext`）| `src/VibrationDashboard/Services/Realtime/MachineNotifier.cs:38` |

## 後端：分層與資料來源切換

| 講稿提到 | 程式碼位置 |
|---|---|
| 表現層（薄 Controller）| `src/VibrationDashboard/Controllers/Api/MachinesController.cs` |
| 邏輯層 | `src/VibrationDashboard/Services/Machines/MachineService.cs` |
| 共用介面（契約）| `src/VibrationDashboard/DataSources/IMachineDataSource.cs` |
| JSON 實作（主）| `src/VibrationDashboard/DataSources/JsonFileDataSource.cs` |
| SQL 實作（可切換）| `src/VibrationDashboard/DataSources/Sql/SqlMachineDataSource.cs` |
| 切換邏輯（唯一的 if/else）| `src/VibrationDashboard/Program.cs:78-96` |
| `DataSource:Type` 設定 | `appsettings.json`(Json) / `appsettings.Development.json`(Sql + Windows 驗證連線字串) / `appsettings.Production.json`(Json) |
| EF 設定 / 映射 / Migrations | `src/VibrationDashboard/Data/`（AppDbContext、Configurations、Entities、Migrations）|
| 模型四層分離 | 領域 `Models/Machine.cs`、持久化 `Data/Entities/`、對外 `DTOs/Responses/`、JSON `DataSources/Json/MachineFileModel.cs` |
| 端到端證明（JSON 種子進 SQL）| `src/VibrationDashboard/Data/SqlDbInitializer.cs:39` |

## BI 面板與領域分析

| 講稿提到 | 程式碼位置 |
|---|---|
| KPI + 狀態分布 + 健康率走勢（30 期滑動視窗）| `src/VibrationDashboard/wwwroot/js/panels.js:15`(MAXH=30)、`:98-112` |
| 風險排行 Severity（值÷危險門檻）| 後端算 `Services/Machines/MachineService.cs:72`、前端排序 `wwwroot/js/panels.js:118-171` |
| 狀態判定（警告/危險兩門檻）| `src/VibrationDashboard/Common/Helpers/StatusEvaluator.cs:19-24` |
| 趨勢（最小平方法線性回歸）| 後端 `Common/Helpers/MeasurementAnalyzer.cs`、前端鏡像 `trendChart.js` 的 `slopePerDay` |

## 清單功能

| 講稿提到 | 程式碼位置 |
|---|---|
| 搜尋 / 篩選 / 分頁（前端對全量）| `src/VibrationDashboard/Views/Home/_FilterBar.cshtml` + `wwwroot/js/dashboard.js` |

## 工程品質

| 講稿提到 | 程式碼位置 |
|---|---|
| 單元測試 | `tests/VibrationDashboard.Tests/`（StatusEvaluator / MeasurementAnalyzer / MachineService / JsonFileDataSource Tests）|
| 整合測試（完整 HTTP/SignalR 管線）| `tests/VibrationDashboard.Tests/Integration/`（MachinesApi / Hub / Smoke）|
| 統一 ProblemDetails 錯誤格式 | `src/VibrationDashboard/Middleware/ExceptionMiddleware.cs` + `Common/Exceptions/AppException.cs` |
| 安全標頭 + CSP | `src/VibrationDashboard/Program.cs:127-129` |
| 圖檔 MIME 白名單（擋 SVG）| `src/VibrationDashboard/DataSources/JsonFileDataSource.cs:151-176` |
| 設備 id 白名單（防路徑穿越）| `src/VibrationDashboard/DataSources/JsonFileDataSource.cs:191-200` |
| 圖檔 ETag / 304 | `Controllers/Api/MachinesController.cs:42-54` + `Services/Machines/MachineService.cs:101-106` |

## AI 協作（SDD）

| 講稿提到 | 位置 |
|---|---|
| 規格文件（需求 / 架構 / 資料 / 即時 / 視覺 / 領域）| `docs/`（01–08）|
| 開發規則文件 | `rules/`（前端實作準則、後端實作準則、命名規範）|

---

> 提醒:報告當天上場前先關掉執行中的 app，再跑 `dotnet test` 確認 **41 支全綠**（單元 + 整合）。
